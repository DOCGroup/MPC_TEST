#if defined(ALSO_REQUIRED)
The ALSO_REQUIRED macro should not be set
#endif
#if !defined(REQUIRED)
The REQUIRED macro should be set
#endif
#if !defined(NEEDED_BY_ALL)
The NEEDED_BY_ALL macro should be set
#endif
#if defined(NEEDED_BY_NONE)
The NEEDED_BY_NONE macro should not be set
#endif
